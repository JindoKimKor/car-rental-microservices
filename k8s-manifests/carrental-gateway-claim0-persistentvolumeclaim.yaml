apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: carrental-gateway-claim0
  name: carrental-gateway-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
