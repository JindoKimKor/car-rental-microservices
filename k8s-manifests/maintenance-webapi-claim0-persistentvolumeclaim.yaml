apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: maintenance-webapi-claim0
  name: maintenance-webapi-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
