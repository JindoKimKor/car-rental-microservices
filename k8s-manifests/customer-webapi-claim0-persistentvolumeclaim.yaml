apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: customer-webapi-claim0
  name: customer-webapi-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
